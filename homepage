#返回模块中上一模块数据是否出现在了下一模块中
#基本思路
# 获取模块一数据
# 拿出模块一中的每个数据
# 拿出模块二中的每个数据与模块一中的数据做对比
# 若相等，输出测试结果为Fail及该数据值
a = [1, 2, 3]
b = [5, 1, 6, 3]
for i in a:
    m = i
    for j in b:
        n = j
        if m == n:
            print("Fail "+str(m))
        else:
            continue
